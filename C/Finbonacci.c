#include <stdio.h>

int main()
{
    int i;
    printf("enter the no:\n");
    scanf("%d", & i);
    int a = 0;
    for (;a <= i;a++)
    {
        int s;
        s = s + ;
        if (i == a)
        {
            printf("fibonacci series of no %d is=%d", a, s);
        }
    }

    return 0;
}
